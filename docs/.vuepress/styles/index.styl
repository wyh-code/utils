
.dark .content__default code {
  background-color: rgba(58,58,92,0.7);
  color: #fff;
}

a.sidebar-link {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  position: relative; 
}

